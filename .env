# .env
# Where you push from the host
PUSH_REGISTRY=localhost:5001

# How Clair (inside its container) should reach that same registry
# On Docker Desktop (Windows/macOS) this works out-of-the-box:
PULL_REGISTRY_FOR_CLAIR=host.docker.internal:5001
